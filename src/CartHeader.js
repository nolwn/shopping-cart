import React from "react";

const CartHeader = () =>
  <nav className="navbar navbar-dark bg-primary">
    <span className="navbar-brand" href="#">Shopping Cart</span>
  </nav>;

export default CartHeader;
